'use strict';

describe('myApp.homeView module', function() {

  beforeEach(module('myApp.homeView'));

  describe('view1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('HomeViewCtrl');
      expect(ctrl).toBeDefined();
    }));

  });
});
